syntax = "proto3";

option go_package = "shortvid-backend/api/user-service/v1;v1";

import "errors/errors.proto";

enum ErrorReason {
	// 设置缺省错误码
	option (errors.default_code) = 500;
  
	// 用户相关
	USER_NOT_FOUND = 0 [(errors.code) = 404];
}
syntax = "proto3";

package errors.v1;
option go_package = "shortvid-backend/api/v1/errors;errorsV1";

import "errors/errors.proto";

enum ErrorReason {
	// 设置缺省错误码
	option (errors.default_code) = 500;
  
	// 用户相关
	USER_NOT_FOUND = 0 [(errors.code) = 404];
}